CREATE TABLE icon (
                      icon_id BIGINT AUTO_INCREMENT,
                      icon_image_url VARCHAR(255),
                      PRIMARY KEY (icon_id)
);

INSERT INTO icon VALUES (1,"toastImageUrl1");
INSERT INTO icon VALUES (2,"toastImageUrl2");